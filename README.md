# AeroTwin — AI-Powered Digital Twin Construction Monitoring Platform

A full-stack web platform for intelligent construction site monitoring, using drone video analysis, AI-simulated object detection, a 3D interactive Digital Twin viewer, and automated safety compliance reporting.

---

## Project Structure

```
New folder/
├── backend/          # FastAPI Python server
│   ├── main.py       # API routes (auth, projects, videos, alerts, reports)
│   ├── database.py   # SQLModel ORM + SQLite schema
│   ├── ai_processor.py  # Simulated YOLOv11 analysis background task
│   ├── reports.py    # HTML report generation (daily/weekly/monthly)
│   └── .venv/        # Python virtual environment
└── frontend/         # Next.js React TypeScript app
    └── src/
        ├── app/
        │   ├── globals.css     # Premium dark UI design system
        │   ├── layout.tsx      # Root layout + SEO metadata
        │   └── page.tsx        # Main dashboard cockpit page
        └── components/
            ├── DigitalTwinViewer.tsx    # Three.js 3D construction model
            └── VideoAnalysisOverlay.tsx # CV bounding box feed player
```

---

## Running the Application

### 1. Start the FastAPI Backend

```powershell
cd backend
.venv\Scripts\python -m uvicorn main:app --host 127.0.0.1 --port 8000
```

Backend auto-seeds:
- 2 example projects (*Metro Tower Phase 2*, *Riverside Bridge Expansion*)
- 4 user accounts (admin, manager, engineer, viewer)
- 1 drone flight log with 36 GPS coordinates
- 10 AI telemetry frames + 2 safety alerts

### 2. Start the Next.js Frontend

```powershell
cd frontend
npm run dev
```

Open **http://localhost:3000** in your browser.

---

## Demo Login Credentials

| Role | Username | Password |
|------|----------|----------|
| Admin | `admin_user` | `password` |
| Project Manager | `manager_user` | `password` |
| Engineer | `engineer_user` | `password` |
| Viewer | `viewer_user` | `password` |

---

## Features

### 🏗️ Dashboard (3D Digital Twin HUD)
- Interactive Three.js 3D construction site
- Building stages visually constructed by timeline slider (0–100%)
- Animated tower crane and drone flight path
- BIM element inspector (click legend items for metadata)
- KPI cards: progress %, PPE compliance, safety alerts, active equipment

### 🎥 CCTV & Drone Feeds
- Upload site videos or CCTV footage
- Trigger AI analysis (simulated YOLOv11 pipeline)
- Real-time progress polling (processing → completed)
- Bounding box overlays for workers (green = PPE OK, red = violation) and machinery

### 📊 Report Center
- Generate daily, weekly, or monthly HTML reports
- Statistics: worker counts, violation breakdown, detected stages, drone logs
- Print/export via browser print dialog

### ⚙️ Project Config
- Create new project workspaces
- Upload video feeds per project
- View drone coordinate mapping

---

## API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/auth/login` | Login user |
| POST | `/auth/register` | Register new user |
| GET | `/projects` | List all projects |
| POST | `/projects` | Create project |
| GET | `/projects/{id}/videos` | List project videos |
| POST | `/projects/{id}/videos` | Upload video |
| POST | `/videos/{id}/analyze` | Trigger AI analysis |
| GET | `/videos/{id}/telemetry` | Get detection frames |
| GET | `/videos/{id}/alerts` | Get safety alerts |
| POST | `/alerts/{id}/resolve` | Resolve an alert |
| GET | `/projects/{id}/flights` | Get drone flights |
| GET | `/projects/{id}/reports/{type}` | Generate report |

Full docs at **http://127.0.0.1:8000/docs** (Swagger UI auto-generated by FastAPI).

---

## Phase 2 Integration Roadmap

- BIM/Revit IFC model import
- Live IoT sensor feeds (RFID, temperature, vibration)
- CesiumJS GIS terrain overlay
- Smart camera live-stream WebRTC integration
- HVAC / Fire Alarm / Access Control modules
# twinz360
